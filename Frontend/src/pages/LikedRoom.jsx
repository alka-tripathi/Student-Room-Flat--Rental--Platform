// import React from 'react';
// import LikedCards from '../components/LikedCards';

// function LikedRoom({ likedRooms, setLikedRooms }) {
//   return (
//     <div>
//       <h1>Liked page</h1>
//       {likedRooms.length === 0 ? (
//         <p>No liked rooms yet ❤️</p>
//       ) : (
//         <div className="cards-container">
//           {likedRooms.map((room) => (
//             <LikedCards
//               key={room._id}
//               room={room}
//               setLikedRooms={setLikedRooms}
//               likedRooms={likedRooms}
//             ></LikedCards>
//           ))}
//         </div>
//       )}
//     </div>
//   );
// }

// export default LikedRoom;
